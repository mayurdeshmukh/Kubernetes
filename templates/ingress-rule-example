apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: apache-ingress
 annotations:
  nginx.ingress.kubernetes.io/rewrite-target:/
 
spec:
  rules:
  - http:
     path:
     - path: /mayur
       backend:
        serviceName: nginx-ingress
        servicePort: 80
